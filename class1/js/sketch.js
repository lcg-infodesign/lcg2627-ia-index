function setup() {
  createCanvas(400, 400); // create a 400x600 window
}

function draw() {
  background(100); // dark blue background (like space)

  circle(200, 200, 100); // draw a circle at (200, 200) with a diameter of 100
}